apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-configuration-parameters
  namespace: dreem
data:
  minNodes: "1"
  maxNodes: "3" #worker + cp
  selectionProfile: "ENERGY" # NEUTRAL, QOS, ENERGY

