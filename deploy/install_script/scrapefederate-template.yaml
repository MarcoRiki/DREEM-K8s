- job_name: 'federate-scrape'
  metrics_path: /federate
  params:
    match[]:
      - node_cpu_seconds_total
      - node_memory_MemAvailable_bytes
  static_configs:
    - targets:
        -  {{CONTROLPLANE_IP}}