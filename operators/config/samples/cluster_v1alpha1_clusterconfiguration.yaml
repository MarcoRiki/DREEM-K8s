apiVersion: cluster.dreemk8s/v1alpha1
kind: ClusterConfiguration
metadata:
  labels:
    app.kubernetes.io/name: operators
    app.kubernetes.io/managed-by: kustomize
  name: clusterconfiguration-sample3
  namespace: dreem
spec:
  maxNodes: 5 # worker + control-plane
  minNodes: 1 # worker + control-plane
  requiredNodes: 1 # worker

